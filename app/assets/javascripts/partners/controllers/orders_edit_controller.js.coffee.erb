TeamDays.OrdersEditController = Ember.ObjectController.extend({
  statuses: <%= Order::STATUSES%>
  actions: {
    update: (order)->
      order.set 'status', @get('status')
      order.save().then  =>
        @transitionToRoute('orders')
  }
})